title = "DevSecOps Lab Rules"

[[rules]]
id = "generic-api-key"
description = "Detect API keys"
regex = '''(?i)(api_key|secret|token|password|aws_secret_access_key)\s*=\s*["'][A-Za-z0-9\/+=]{16,}["']'''
tags = ["secret"]
